declare global {
    namespace App {
        // interface Error {}
        // interface Locals {}
        // interface PageData {}
        // interface Platform {}
    }
    type TileProps = {
        x: number,
        y: number,
        colour: string
    }
}

export {};
